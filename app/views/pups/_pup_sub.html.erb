<div class="col-md-2">  
  <%= image_tag pup.pic, class: 'img-responsive' %>  
</div>

<div class="col-md-10">
  <div class="col-md-12">
    <h2><%= "#{pup.name}" %>
      <%= gender_icon pup %>
      <%= " the #{pup.age} year-old #{pup.breed}" %>
    </h2>
  </div>
</div>

<div class="col-md-10">
  <div class="col-md-12">
    <h4>
      Size: <%= size_icon pup %>
      Energy Level: <%= energy_icon pup %>
      Walks Completed: <%= pup.walks_completed %>
    </h4>
  </div>
</div>
